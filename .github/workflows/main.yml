on:
  push:
    paths:
      - 'apps/wordpress/**'
name: 🚀 Deploy WordPress Theme on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: web0167.zxcs.nl
        username: u200754p400290
        password: ${{ secrets.ftp_password }}
        server-dir: /home/u200754p400290/domains/wp.rawshaping.com/public_html/wp-content/themes/raw13/